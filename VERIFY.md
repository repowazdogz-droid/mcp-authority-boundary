# Verify every talk figure in about a minute

Expected lines below are the ones observed on 2026-09-10 (macOS, Node 26.8.1) at
commit `4e1f074` of branch `hardening-2026-09-10`; they are also what CI prints
(`.github/workflows/verify.yml`). Needs Node 20.11+, git, Python 3 with sqlite3. The clone must not be shallow:
two tests rebuild `631196d` from history (a `fetch-depth: 1` checkout fails them).

```bash
git clone -b hardening-2026-09-10 https://github.com/repowazdogz-droid/mcp-authority-boundary.git
cd mcp-authority-boundary && npm ci
```

| Command | Expected output line |
|---|---|
| `npm test` | `ℹ tests 182` / `ℹ pass 182` / `ℹ fail 0` (169 before this branch; 177 before the grant-race and TOCTOU tests) |
| `npm run experiment:hardening` | `threat model caller-only: 9 file cases, 240 SQL cases` / `threat model executor-compromise: 10 file cases, 0 SQL cases`, then `{"output":...,"sqlCases":240,"fileCases":19,"passed":true}`; RESULTS.json: 240 SQL / 0 mismatches, 19 file cases, `transient-extra-write` marked `knownMiss` |
| `node scripts/manifest-digest.mjs` | `manifest-digest fb3d1735ca5b2f130fc158164d0f800a81059e9124617335b61af677ce5280d3` then `77 files ... 0 differ from the working tree` (was `e2b829ad…` over 71 files at `de90b23`) |
| `npm run test:formal` (optional; Lean 4.33.1 via elan) | `formal: formal/vectors.json regenerated by Lean, 7 vectors, byte-identical to the committed copy`; axioms only `propext`, `Quot.sound`, no `sorryAx` |
| `npm run test:conformance` (optional; Lean) | `conformance: OK — 39 traces, 3199 events, 0 unexpected non-conforming, 2 expected (executor-compromise mutants: no-write, shadow); trace check, not a refinement proof` |
| `node scripts/bench.mjs` | a 5-stage × 3-size table of median/p95 ms; on this machine (M4 Max) `decide` ≈ 2.8 ms at every size and `resolve`/`digest`/`consume` ≈ 0.6–1.1 ms at 1 MB. One machine; numbers will differ elsewhere |

The counterexample needs the two frozen versions built once (network for `npm ci`):

```bash
git worktree add /tmp/mab-exp/v1 631196d && (cd /tmp/mab-exp/v1 && npm ci && npm run build)
git worktree add /tmp/mab-exp/head 7b7e973 && (cd /tmp/mab-exp/head && npm ci && npm run build)
bash experiments/independent-observer/run-all-local.sh
```

Expected: the two-row table, and in the observer line for v1
`"fileBytes":100000,"fileSha256":"d69e68988157833272305aaf21f453c800346e8a3640db6578e260215542e5d4"` ... `"comparison":"DIVERGENCE"`.

| Version | Cedar | Ledger / replay | Independent world observer |
|---|---|---|---|
| v1 (631196d) | ALLOW (byteLen=0) | VERIFIED | 100000 bytes written vs 0 authorized -> DIVERGENCE |
| head (7b7e973) | REFUSE (unresolvable-resource) | INCOMPLETE (nothing executed) | no write |

The two frozen commits: `631196d` is v1, 66 tests green, replay VERIFIED, and the
100,000-byte write executes under a 4,096-byte cap. `7b7e973` is the repaired base
of this branch, 155 tests green, and the same call is refused before Cedar. The single-
host run above is the only run this repository records; `npm test` also proves the
harness export the observer reads is byte-identical to v1's in-memory effect
(`test/observer-export.test.ts`).
