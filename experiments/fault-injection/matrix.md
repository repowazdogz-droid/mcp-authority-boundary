| Injected defect | Existing tests | Cedar | Replay | Binding check | Independent observer |
|---|---|---|---|---|---|
| D1 executor uses raw input | PASS 155/0 | unexpected-type input refused / plain input allowed (masked upstream) | ALL STAGES PASS | PASS | AGREE |
| D2 grant-digest check removed | PASS 155/0 | allow (for input A; grant presented with input B) | n/a (no ledger on this probe path) | n/a | no write (grant presented with input B is stopped at the linkage check) |
| D3 resource altered post-auth | FAIL 135 pass / 20 fail (e.g. T1b the amplification is real: B ends up holding finance content it cannot read; T4b the repair re-routes the function rather than deleting it; A1c the ledger now records the canonical operation and both effect fingerprints) | allow, then fail-loud THROW | n/a (no ledger on this probe path) | n/a | EFFECT_WITHOUT_RECORD (stray .shadow file, empty ledger) |
| D4 coercion reintroduced | FAIL 152 pass / 3 fail (e.g. A1 [WAS EXPLOITABLE, NOW BLOCKED] a non-string payload is refused before Cedar; A8 the A1 divergence can no longer be recorded, so there is no such ledger to replay; F1 every string field of every tool rejects every non-string, fail-closed) | ALLOW byteLen=0 | ALL STAGES PASS | PASS | AGREE (0 vs 0) |
| D5 grant reuse allowed | PASS 155/0 | allow | n/a (no ledger on this probe path) | n/a | EFFECT_WITHOUT_RECORD (11B written, zero ledger lines) |
| D6 effect omitted | FAIL 135 pass / 20 fail (e.g. T1b the amplification is real: B ends up holding finance content it cannot read; T4b the repair re-routes the function rather than deleting it; A1c the ledger now records the canonical operation and both effect fingerprints) | allow, then fail-loud THROW | n/a (no ledger on this probe path) | n/a | NO_EXECUTION_NO_WRITE |
| D9 combined variant (D1+D4) | FAIL 152 pass / 3 fail (e.g. A1 [WAS EXPLOITABLE, NOW BLOCKED] a non-string payload is refused before Cedar; A8 the A1 divergence can no longer be recorded, so there is no such ledger to replay; F1 every string field of every tool rejects every non-string, fail-closed) | allow at resolution, then THROW at linkage | n/a (no ledger on this probe path) | n/a | NO_EXECUTION_NO_WRITE |
| D7a mediation record edited | n/a (post-hoc ledger edit) | n/a | FAIL (2 findings) | PASS | AGREE (effect matches; the edit is in mediation metadata) |
| D7b mediation edited + chain re-hashed | n/a (post-hoc ledger edit) | n/a | ALL STAGES PASS | PASS | AGREE (fully invisible) |
| D8 ledger truncated | n/a (post-hoc ledger edit) | n/a | ALL STAGES PASS | PASS | EFFECT_WITHOUT_RECORD (truncated target) / AGREE (retained control) |
